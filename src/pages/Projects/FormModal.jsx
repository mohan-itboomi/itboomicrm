import { Stack, Typography } from "@mui/material";
import { useEffect, useState } from "react";
import { useDispatch } from "react-redux";
import ReusableButton from "../../Component/ReusableButton";
import ReusableInput from "../../Component/ReusableInput";
import CustomDropdown from "../../Component/CustomDropdown";
import { closeModal } from "../../Api/Redux/Reducers/modalSlice";
import { ApiService } from "../../Api/ApiService";
const statuses=["Not Started","On Board","In Progress","On Hold","Completed","Cancelled"].map(value=>({value,label:value})); const priorities=["Low","Medium","High","Critical"].map(value=>({value,label:value})); const initial={projectCode:"",code:"",name:"",client:"",description:"",coordinator:"",teamLead:"",managerId:"",startDate:"",endDate:"",duration:"",status:"Not Started",priority:"Medium"};
const calculateDuration=(startDate,endDate)=>{if(!startDate||!endDate)return "";const start=new Date(startDate);const end=new Date(endDate);if(Number.isNaN(start.getTime())||Number.isNaN(end.getTime())||end<start)return "";const diffDays=Math.ceil((end-start)/(1000*60*60*24))+1;return `${diffDays} day${diffDays===1?"":"s"}`;};
export default function ProjectsFormModal({record,onSaved}){const dispatch=useDispatch();const [values,setValues]=useState({...initial,...record,coordinator:record?.coordinator?._id||record?.coordinator||"",teamLead:record?.teamLead?._id||record?.teamLead||"",managerId:record?.managerId?._id||record?.managerId||"",startDate:record?.startDate?String(record.startDate).slice(0,10):"",endDate:record?.endDate?String(record.endDate).slice(0,10):"",duration:record?.duration||""});const [users,setUsers]=useState([]);const [saving,setSaving]=useState(false);useEffect(()=>{if(!record)ApiService.getProjectId().then(r=>update("projectCode",r.data?.data?.projectCode||"" )).catch(()=>{});ApiService.getEmployeeDropdown().then(r=>setUsers(r.data?.data||[])).catch(()=>setUsers([]));},[]);const update=(f,v)=>{const next={...values,[f]:v};if(f==="startDate"||f==="endDate")next.duration=calculateDuration(next.startDate,next.endDate);setValues(next);};const submit=async e=>{e.preventDefault();setSaving(true);const payload={...values,duration:calculateDuration(values.startDate,values.endDate)};Object.keys(initial).forEach(k=>{if(payload[k]===""||payload[k]===null)delete payload[k];});try{if(record?._id)await ApiService.updateProject(record._id,payload);else await ApiService.createProject(payload);await onSaved?.();dispatch(closeModal());}finally{setSaving(false);}};return <form onSubmit={submit}><Stack spacing={2} sx={{p:1,borderTop:"4px solid #6a1b9a"}}><Typography variant="h6">{record?"Edit Project":"Create Project"}</Typography><ReusableInput label="Project ID" value={values.projectCode} disabled={!record || Boolean(values.projectCode)} onChange={e=>update("projectCode",e.target.value)}/><ReusableInput label="Project Code" value={values.code} onChange={e=>update("code",e.target.value)}/><ReusableInput label="Project Name" value={values.name} onChange={e=>update("name",e.target.value)} required/><ReusableInput label="Client" value={values.client} onChange={e=>update("client",e.target.value)}/><ReusableInput label="Description" value={values.description} onChange={e=>update("description",e.target.value)} multiline minRows={3}/><CustomDropdown label="Coordinator" value={values.coordinator} options={users} onChange={v=>update("coordinator",v)}/><CustomDropdown label="Team Lead" value={values.teamLead} options={users} onChange={v=>update("teamLead",v)}/><CustomDropdown label="Manager" value={values.managerId} options={users} onChange={v=>update("managerId",v)}/><ReusableInput label="Start Date" type="date" value={values.startDate} onChange={e=>update("startDate",e.target.value)} InputLabelProps={{shrink:true}}/><ReusableInput label="End Date" type="date" value={values.endDate} onChange={e=>update("endDate",e.target.value)} InputLabelProps={{shrink:true}}/><CustomDropdown label="Status" value={values.status} options={statuses} onChange={v=>update("status",v)} required/><CustomDropdown label="Priority" value={values.priority} options={priorities} onChange={v=>update("priority",v)} required/><ReusableButton title={record?"Update Project":"Create Project"} type="submit" loading={saving}/></Stack></form>}
